{
  "name": "webchat-pages",
  "version": "1.0.0",
  "description": "WebChat Frontend - Cloudflare Pages",
  "scripts": {
    "build": "echo 'No build step required'",
    "preview": "wrangler pages dev ."
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}